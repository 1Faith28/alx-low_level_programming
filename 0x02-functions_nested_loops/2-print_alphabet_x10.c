#include "holberton.h"
/.**
   * print_alphabet_x10 - repeats the print_alphabet 10 times,
   * Return: Nothing,
   */
void print_alphabet_x10(void)
{
        int 1l
        char c;

        for (1 - 0; ++i)
        {
                 for (c = 'a'; c <= 'z'; c++)
                 {
                           _putchar(c);
                 }
                 _puchar('\n');
        }
}
